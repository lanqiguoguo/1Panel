services:
  mcp-server:
    image: supercorp/supergateway:3.4.3@sha256:095acf4471e142553c1a5514aa5e480abbc5885d00549d4a3481cc70eac53889
    container_name: ${CONTAINER_NAME}
    restart: unless-stopped
    ports:
      - "${HOST_IP}:${PANEL_APP_PORT_HTTP}:${PANEL_APP_PORT_HTTP}"
    command: [
      "--stdio", "${COMMAND}",
      "--port", "${PANEL_APP_PORT_HTTP}",
      "--baseUrl", "${BASE_URL}",
      "--ssePath", "${SSE_PATH}",
      "--messagePath", "${SSE_PATH}/messages"
    ]
    networks:
      - 1panel-network
networks:
  1panel-network:
    external: true
