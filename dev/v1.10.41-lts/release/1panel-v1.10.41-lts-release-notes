# v1.10.41-lts

本版本包含 5 轮全库安全审查的累计修复：覆盖命令注入、路径穿越、凭据泄露、认证与会话、前端注入等 50 余项问题。**普通用户（面板直连、无 API 自动化）升级无感；反向代理用户与有 API 自动化的用户请阅读文末「升级注意事项」。**

## 安全加固

### 命令注入

- **修复 SSH 登录日志搜索命令注入**：搜索关键字含 shell 元字符时直接拒绝，杜绝单引号逃逸执行任意命令
- **修复系统快照加密口令命令注入**：快照口令与归档参数在压缩前统一校验（含计划任务存量记录的运行时兜底）
- **修复 SSL/网站日志/升级版本等输入注入**：网站日志类型白名单化、升级版本号字符白名单、MySQL 配置变量拒绝换行与注释符
- **修复 tar 排除规则选项注入**：排除规则与归档目录统一单引号包裹，命令校验拒绝 Tab 等不可见分隔符，阻止 GNU tar checkpoint 劫持
- **修复应用商店镜像名注入**：compose 镜像引用白名单校验并改为参数化拉取，阻止被劫持商店下发 `$(...)` 载荷在安装/升级时执行
- **修复 SSH 设置项注入**：设置项收敛为白名单枚举（端口数字、开关布尔、地址 IP），阻止经 semanage/sshd_config 注入命令或新增指令
- **修复 TestCompose 编排名路径穿越**：名称校验下沉到共享写入点，阻止任意目录创建与同名文件覆盖

### 凭据泄露

- **数据库/Redis 凭据移出进程命令行**：本地与远程 MySQL 备份恢复、终端 redis-cli 全部改经 0600 环境文件传递，`/proc` 不再可见
- **归档加密口令移出命令行**：openssl 加密/解密全部经文件描述符传递口令，加密与解密双向兼容旧格式
- **凭据不再明文回显**：备份账号、DNS 账号、CA 私钥、ACME EabHmacKey、面板 ApiKey 的列表/详情响应统一掩码，提交掩码或空值保留存量
- **私钥文件权限收紧**：站点/CA/下载临时目录内的私钥统一 0600，存量宽权限文件自动收紧
- **备份账号凭据掩码 fail-closed**：历史损坏数据不再原样回显

### 路径穿越与文件保护

- **修复工具箱磁盘清理任意删除**：清理目标严格校验并复用受保护路径检查，阻止删除面板目录之外的任意路径
- **受保护路径扩展到写操作**：面板/系统关键目录的改名、保存、移动、权限、属主修改与删除同等拦截
- **修复下载与日志读取穿越**：系统日志名强制日期格式、网站日志类型白名单、下载端点拒绝符号链接并复核打开结果、在线读取拒绝字符设备与管道（不再挂死）
- **归档安全**：压缩包条目拒绝穿越/符号链接/根条目、限制嵌套深度与总量、剥离 setuid 等特权位、上传恢复限定面板数据目录内
- **修复回收站还原目标校验**：还原来源限定回收站目录，拒绝任意路径；修正挂载点前缀误判

### 认证与会话

- **API-Key IP 白名单改用真实连接地址**：不再信任 X-Forwarded-For，白名单无法被伪造头绕过（详见升级注意事项）
- **WebSocket 来源校验含端口**：同 IP 不同端口的站点无法借同站 Cookie 发起跨站 WebSocket 劫持（详见升级注意事项）
- **修复 MFA 限流与状态**：限流解锁需验证码、失败后状态机复位、登录页不再重复请求
- **API 接口启用门禁**：未配置 ApiKey 时拒绝启用；ApiKey 列表掩码回显、启用前强校验
- **安全材料改用加密随机源**：JWT 签名密钥、数据库加密密钥、ApiKey 与初始用户名/密码/安全入口改用 crypto/rand 生成

### 前端

- **修复终端自动跳转路径注入**：URL `?path=` 白名单校验，点击恶意链接不再能在主机执行命令
- **登出与 401 清理会话足迹**：路由缓存、标签页记录、MFA 状态全面清理；登出等待服务端确认
- **外链加固**：应用商店/域名外链仅放行 http(s) 并隔离 opener
- **安全随机与内存卫生**：入口码/私钥口令/数据库密码改加密随机源；Blob 下载及时释放；容器终端查询串编码

## 修复

- **修复快照管线状态机**：失败快照的目录/压缩包清理、应用数据并发缺陷、状态写入失败不再静默
- **修复降级/升级路径**：升级包 sha256 校验失败即中止、1pctl 参数迁移、bootstrap 密码保护与日志脱敏、下载脚本隔离
- **修复上传与合并内存占用**：分片上传/合并改为流式，峰值内存由单文件大小降为常数级
- **修复守护进程与命令健壮性**：supervisor numprocs 限界、fail2ban 解禁回滚 IP 过滤、gunzip 参数化、出站请求体句柄回收
- **修复 nginx 指令注入与配置回滚**：反代/重定向/防盗链等参数统一校验；daemon.json 写入失败回滚
- **修复 OneDrive 硬编码凭据**：不再向新装实例预置公开凭据，表单改由用户填写
- **修复 MCP 网关镜像固定**、容器日志跟随进程泄漏、监控采集边界、应用商店同步死锁等稳定性问题

## 前端修复

- **修复路由会话缓存**：登录/登出/401/403 全路径清理，路由权限以服务端会话为准
- **修复入口页错误状态残留**：访问错误入口后修正地址刷新不再永久卡在错误页
- **修复镜像构建日志错误详情丢失**、登录页键盘监听清理、开发模式 Vite 默认绑定回环地址

## 升级注意事项

- **反向代理用户（重要）**：面板 WebSocket 现在校验来源含端口号。代理必须把 Host 原样传给面板（`proxy_set_header Host $http_host;`；TLS 终结在 443 的写 `Host $host:443;`），否则终端/容器执行等 WebSocket 功能会断开（面板日志出现 `reject websocket upgrade` 即此原因）
- **反向代理 + API-Key 白名单**：白名单现按面板看到的直连地址判定（与面板允许 IP 语义一致），反代场景需填写代理地址而非终端用户 IP
- **API 自动化脚本**：备份/DNS/CA/ACME/ApiKey 的凭据不再明文回显（提交掩码或空值保留存量）；上述输入校验收紧点若被脚本用到请自查参数形态
- **文件管理器**：受保护目录（面板数据目录与系统关键目录）内的改名/保存/移动/权限修改现在与删除一样被拦截
- `ApiKeyValidityTime=0`（不做时间戳校验）的文档化行为保持不变