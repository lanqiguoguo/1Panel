# v1.10.40-lts

## 安全加固

- **修复 SSH 密钥生成命令注入**：生成密钥时改为参数化调用 ssh-keygen，杜绝通过密码字段注入额外参数覆盖任意文件；同时修正密码设置语义（-N），生成的私钥真正启用口令保护
- **修复防火墙转发规则命令注入**：转发规则的端口/IP/协议改为白名单校验（纯数字端口、合法 IPv4），阻止通过请求体注入 shell 命令
- **修复备份下载路径穿越**：备份记录下载/恢复的目录与文件名严格净化，阻止读取备份目录之外的任意文件
- **修复数据库配置编辑路径穿越**：配置编辑接口的实例名严格校验，阻止读取面板数据目录之外的任意文件
- **修复 Supervisor 进程名注入与配置权限**：进程名按白名单校验，.ini 配置权限由 0755 收紧为 0640
- **修复 fail2ban 配置注入**：配置项值拒绝换行/注释符并按键校验格式（数字、端口、路径），阻止写入非法配置导致服务异常
- **修复备份加密密钥日志泄露**：备份 tar 加密命令中的密钥脱敏覆盖引号包裹形式，不再泄露到调试日志

## 修复

- **修复快照创建失败后文件残留**：删除失败快照记录时同步清理其工作目录、压缩包与恢复临时目录，不再堆积磁盘占用
- **修复应用安装并发缺陷**：安装后台任务不再与主流程共享错误变量，避免失败时目录被并发删除、状态不一致
- **修复容器日志跟随断开后进程泄漏**：关闭日志跟随（或直接断开连接）后 docker logs 进程被正确回收，不再残留僵尸进程
- **修复应用商店同步偶发卡死**：运行时类应用版本数多于应用数时同步不再死锁（商店界面永久"同步中"）
- **修复 Docker 日志配置清空不生效**：清空唯一日志选项后 dockerd 正确重启，配置真正生效
- **修复商店同步状态写失败被吞**：同步完成状态写失败不再静默，避免升级检测永久失效
- **修复监控停止时偶发 panic**：停止监控与采样并发时不再向已关闭通道发送数据导致 panic
- **修复监控负载采集异常**：系统负载读取失败或 CPU 数为 0 时不再 panic/除零，降级为零值
- **修复 nginx 状态解析越界**：nginx_status 输出格式异常时不再数组越界崩溃
- **修复 compose 编辑失败不回滚**：编辑保存过程中写入/环境文件失败时恢复原 compose 文件
- **修复 OneDrive 令牌刷新任务重复注册**：删除账号后重置定时任务 ID，重新创建不再堆积重复任务
- **修复 PHP 运行环境应用列表**：应用商店仅展示 V1 兼容的 PHP 5/7/8，移除不兼容的 V2 结构 PHP 条目

## 前端修复

- **修复 FTP 日志文件大小显示错误**：GB/TB 换算除数错误导致 ≥1MB 文件大小放大 1024 倍
- **修复日志查看翻页失效**：分页加载被误清空，翻页/实时累积恢复正常
- **修复容器列表 CPU/内存列卡"加载中"**：翻页/筛选后统计加载时序错误，改为数据刷新后加载
- **修复容器监控弹窗定时器泄漏**：关闭页面后轮询定时器不再持续请求
- **修复首页监控轮询永久停止**：单次请求失败不再导致监控数据永远冻结
- **修复 MFA 登录重复请求**：输入验证码不再每键触发登录请求
- **修复容器端口跳转 IPv6 解析错误**：IPv6 地址容器点击端口跳转不再打开错误端口
- **修复登录页受限提示白屏**：IP/域名/安全入口受限时显示明确原因而非空白页
- **修复接口错误码处理**：资源不存在/访问受限等错误码正确进入错误处理，不再抛未捕获异常
- **修复 SSL 证书第二个 DNS 服务器校验失效**：非法 IP 现在会被拦截
- **修复容器日志下载取消副作用**：取消下载不再改变日志显示行数