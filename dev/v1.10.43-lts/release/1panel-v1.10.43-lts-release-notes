# v1.10.43-lts

本版本包含第七轮全库安全审查的累计修复（并发状态机、凭据存储、原子恢复等）与若干前端修复。**升级注意事项见文末「升级注意事项」。**

## 安全加固（高危）

- **修复 compose 项目删除可递归删除任意宿主目录**：compose 启停/删除操作的目标路径必须与数据库记录的项目目录一致（或位于约定的 compose 根内），不再能借 delete+withFile 删除任意含 compose 文件的目录树
- **文件保护路径的符号链接解析与移动落点复核**（此前版本加入）随本次写入保护回退一并还原，详见「行为变更」
- **修复应用安装/升级并发互斥**：同名安装、升级、重建、删除按名称串行化（进程内占用 + 数据库状态 CAS），并发失败方不再误删正在安装的应用目录；失败回滚只清理本请求实际创建的资源
- **修复 SSL 证书申请并发重复签发**：证书申请以原子状态抢占（applying）串行化，创建自动申请、手动申请、定时续期三者不再对同一证书并发执行
- **修复 SSL 证书部署写盘破坏**：type=auto 空证书不再可能清空已部署证书；上传/部署前校验证书与私钥配对与 SAN 域名；证书文件原子写入，nginx 校验失败回滚旧证书
- **修复 JWT 会话不可撤销**：签发的 JWT 携带会话版本号，改密/改用户名/MFA/安全入口变更时版本递增，旧令牌立即失效
- **修复快照恢复非原子覆盖**：快照数据先解压到临时目录并做完整性校验，再逐成员原子交换进目标；恢复/回滚/删除按快照互斥；恢复成功后状态正确清理，重启失败不再锁死面板
- **修复登录 RSA 私钥与口令同库存储**：RSA 私钥迁移到 0600 本地文件，数据库仅存加密包装行，快照不再携带私钥文件

## 安全加固（重要）

- **应用更新/升级链 URL 校验**：应用商店 compose 按需下载与面板升级 release notes 均校验 URL 公网性与版本白名单
- **备份产物权限收紧**：数据库备份文件 0600/目录 0750，失败自动清理半成品
- **数据库恢复自愈**：MySQL 恢复前重建空库、失败不静默；Redis 恢复失败自动拉起容器、回滚文件加随机后缀防覆盖
- **删除数据库备份按记录路径清理**：备份/上传目录按数据库行解析，删除前校验符号链接不逃逸
- **防火墙批量操作失败明示**：批量规则部分失败返回明细；更新规则失败回滚旧规则；fail2ban 配置原子写入失败恢复
- **应用商店安装/升级/删除与计划任务竞态修复**：任务运行中删除不再重建孤儿目录；compose 命令带超时不再永久挂起
- **Ollama 模型名入库校验**：容器列表同步的模型名白名单过滤，删除/查询走参数化执行
- **回收站还原目标一致性校验**：还原前删除同名目标仅限与回收条目来源一致的位置
- **面板升级并发互斥**：升级以原子状态抢占，并发升级不再互相覆盖二进制
- **网站创建失败清理**：nginx 配置与目录按别名正确清理，不再遗留幽灵配置

## 修复

- **修复已装应用下拉校验误报**：数据库主键选择框使用无上限 ID 校验，应用 ID 超过 65535 不再提示"请选择必选项"
- **修复 php.ini 上传限制缺失行不追加**、rewrite 配置停止/启动后丢失、容器日志路径越权读取、监控停止时潜在 panic、MCP 更新空域名崩溃
- **install.sh**：Docker 询问改走 TTY、密码留空生成随机密码的提示文案

## 行为变更（重要）

- **文件写入保护回退**：文件管理器写路径（保存/上传/新建/移动/下载）不再拦截面板数据目录与系统目录（与 v1.10.42 之前的删除/回收站保护粒度一致），编辑应用配置（如 Caddyfile）恢复可用；相应地，这些写接口不再阻止向 /etc、面板数据库目录等位置的写入。删除与回收站保护保持不变。安全要求高的部署请限制面板访问来源
- **快照恢复不再要求系统服务单元存在**（恢复流程可在无 systemd 环境完成数据交换）

## 升级注意事项

- JWT 撤销机制上线后，升级前已签发的 JWT 立即失效（前端不使用 JWT，无感知）
- 登录 RSA 私钥已迁移至本地文件，升级后首次启动自动完成迁移，无需人工操作